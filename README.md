# canon_flutter

Flutter bindings for [canon](https://pub.dev/packages/canon): the router
delegate (`NavDelegate`, `graph.delegate`), `ScreenManager`, `ScreenScope`,
and the reactive view-state widgets (`Query.of`, `Fragment.of`, `Placement`).

Consumers import only this package — it re-exports canon.
