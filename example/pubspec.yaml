name: canon_web_nav_example
description: Web navigation, whole and minimal — one grammar tree, no ledger.
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  canon: ^0.29.0
  canon_flutter: ^0.9.0

dev_dependencies:
  build_runner: ^2.4.0
  canon_generator: ^0.35.0
  flutter_test:
    sdk: flutter
